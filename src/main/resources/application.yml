server:
  port: 80

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mmcms?useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver

  mvc:
    view:
      prefix: /
      suffix: .html

  thymeleaf:
    cache: true

  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java